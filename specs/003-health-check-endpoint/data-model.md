# Phase 1 Data Model: Backend Health-Check Endpoint

**Status**: N/A — no data model changes.

This feature introduces no new entities, fields, relationships, validation rules, or state
transitions. The health-check response (`{"status": "ok"}`) is a static literal, not backed
by any persisted or computed model. No database table, SQLAlchemy model, or Pydantic schema
is added or changed.
